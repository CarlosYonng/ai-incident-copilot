# Backend uses ai-agent-infra-stack MySQL.
# Initialize once with: scripts/init-db.sh
# Docker compose hardcodes:
# jdbc:mysql://host.docker.internal:3306/incident_copilot
# incident_copilot / incident_copilot123

# Diagnosis MCP
# Use http://host.docker.internal:8200 when backend runs in Docker and diagnosis-service runs on the host.
DIAGNOSIS_MCP_TOKEN=
